{
  "name": "marketplace-b2b",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api":        "npm run start:dev --prefix backend",
    "web":        "npm start --prefix frontend",
    "db:up":      "docker compose up -d",
    "db:down":    "docker compose down",
    "db:reset":   "docker compose down -v && docker compose up -d",
    "migrate":    "npm run --prefix backend prisma migrate dev",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build:api":  "npm run build --prefix backend",
    "build:web":  "npm run build --prefix frontend"
  }
}
